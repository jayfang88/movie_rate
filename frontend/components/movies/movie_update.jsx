import React from 'react';
import { connect } from 'react-redux';
import MovieForm from './movie_form';

class MovieUpdate extends React.Component {

};

const mstp = state => {
    return {

    }
};

const mdtp = dispatch => ({

});

export default connect(mstp, mdtp)(MovieUpdate);